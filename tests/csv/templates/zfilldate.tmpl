{
    "persons" : [
{%- for person in rows %}
        {
            "group" : "{{person.group}}",
            "number" : "{{person.number}}",
            "date" : "{{person.date | zfilldate}}"
        }{% if not loop.last %},{% endif %}
{%- endfor%}
    ]
}
